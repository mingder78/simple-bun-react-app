import React from "react"
import ReactDOM from "react-dom/client"
import { Button } from "@yourorg/my-react-lib"

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Button />
  </React.StrictMode>
)

